#include <ggl.h>

void gglBlendFunc(GLenum sfactor, GLenum dfactor)
{
    glBlendFunc(sfactor, dfactor);
}

void gglColor4f(GLfloat red, GLfloat green, GLfloat blue, GLfloat alpha)
{
    glColor4f(red, green, blue, alpha);
}

